// config file for use with the airtable import script 'airtable import'
// To use, fill in the values and rename to 'config-airtable.js'

var airtable_config = {
    auth: {
        apiKey: 'API-KEY',
    },
    base_id: 'airtable base id'
};

module.exports = airtable_config;
